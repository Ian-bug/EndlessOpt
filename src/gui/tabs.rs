// Tab-specific UI components
// This module can be expanded to separate tab implementations into their own modules

// For now, tabs are implemented directly in app.rs for simplicity
// As the UI grows, individual tabs can be moved here
